<snippet>
    <content><![CDATA[(function()
{
    'use strict';

    B.${1:Components}.${2:Class} = B.${3:Core}.${4:Abstract}.extend(
    {
    	${5:static  : '${6:class}',
        }options : {${7}},

        init : function( options )
        {
            this._super( options );

    		${8}
        }
    } );
} )();]]></content>
    <tabTrigger>bcs</tabTrigger>
    <scope>source.js</scope>
</snippet>
